# undernote — wave log

프로젝트: undernote (bathos-b8a396ee-6b9e-4275-9de3-f94e7e698431)
컨셉: 묻혀 있던 명반을 찾아 소개하고 평가하는 음악 매거진.

---

## 2026-09-01 — 시퀀스 ③ Scale-Adaptive 라우팅 (/route)

**Stakes (User 응답):**

| 항목 | 값 | 비고 |
|---|---|---|
| scope | D — 신규 제품·대형 | 빈 저장소에서 시작하는 새 제품 |
| novelty | B — 일부 새로운 영역 (medium) | 음악 메타데이터 연동(MusicBrainz/Discogs 등) 미확정 |
| regulation_ip | A — 없음 | 특허·컴플라이언스 대응 계획 없음 |
| team_size | A — 혼자 (solo) | |

**판정:** 매트릭스(scope=D → Lv3)와 엔진 `route decide` 결과 일치. 추천 **Lv3**.

**User 확정: Lv3** — `route decide --confirm 3` 으로 manifest 기록 완료
(`current_level=3`, `routing[0].confirmed_level=3`, `state validate` → VALID).

**가동 웨이브 — User 조정 있음:**

- 엔진 기록상 wave_set: `W0 W1 W2 W3 W4 W5 W6` (Lv3 기본값)
- **실제 가동: `W1 W2 W3 W5 W6`**
- **제외: W0(Analysis 전단) · W4(IP·연구 플러그)** — User 명시 결정.
  근거: team=solo 이고 regulation_ip=A 라 W4는 필수가 아니며(w4_mode=Optional),
  1인 매거진 제품에서 W0 시장분석 전단은 산출물 대비 소모가 크다고 판단.
  → 발견(W1)부터 시작하되 Lv3의 거버넌스 강도와 W3 스토리 게이트는 유지한다.

> ⚠️ 엔진 wave_set 과 실제 가동 웨이브가 다르다. 이 문서가 W0/W4 제외의 근거 기록이며,
> 이후 웨이브 진입 시 W0·W4 미실행은 드리프트가 아니라 확정된 스코프다.

**스폰 역할(Lv3, 동시 ≤3):** John · Caleb · Joshua · James · Jonnathan · Matthew ·
Phillip · Andrew · Stephen · Thomas · Timothy · Matthias · Michael · Hananiah · Martin
(Mark · Nathanael 은 W4 제외에 따라 미가동)

**#17 Matthew(스토리 엔지니어):** ✓ 가동 (W3)

---

## 2026-09-01 — 킥오프 (/team-kickoff)

리드 Paul 단독 수행. 팀원 미스폰.

**인자 파싱 이슈:** 커맨드 인자에서 경로와 컨셉 사이 공백이 둘이라 `$1` 이 `묻혀` 로
잘렸다. 실제 대상 `D:\su\Documents\Github\undernote` 로 진행했으며 `묻혀/` 디렉터리는
생성하지 않았다.

**컨셉 변경 — 이번 킥오프의 핵심:**
User가 범위를 넓혔다. "묻혀 있던 명반"에서 → **"음반 전반. 묻힌 것이 아니어도 됨."**
charter §1 은 절충안으로 *대상은 넓히되 편집 각도로 차별화한다* 를 채택했으나,
이는 리드의 제안일 뿐 검증되지 않았다. **charter §7-A1 로 등록. W1 최우선 안건.**

**수행 결과:**

| 단계 | 결과 |
|---|---|
| 1) 디렉터리 골격 | 이미 존재 (/route 4단계에서 선행 생성) |
| 2) `00-plan/charter.md` | ✓ 신규 작성 — 컨셉·목표·성공기준·범위/비범위·제약·가정 A1~A5·타깃 |
| 3) `00-plan/task-graph.md` | ✓ 신규 작성 — W1·W2·W3·W5·W6 분해 + 의존성 + W5 소유 경계 초안 |
| 4) `_state/manifest.json` | 이미 시딩됨. 재실행 시 `[E-STATE-EXISTS]` 비파괴 거부 확인. **`--force` 미사용** — 강제 시 Lv3 확정 기록이 소실된다. `state validate` → VALID |
| 5) `_state/wave-log.md` | 이 항목 (덮어쓰지 않고 이어붙임 — 라우팅 기록 보존) |

**W5 파일 소유 경계 초안:** Phillip `src/lib/**` `src/data/**` `content/**` ·
Andrew `src/routes/**` `src/components/**` `src/styles/**` · Stephen 미할당(A4 판정 대기).
공유 설정 파일은 Phillip 단독 소유. **스택 미정이라 W2(2.3) 확정 후 실제 경로로 치환 필요.**
불변 조건: 소유 경로 교집합 = 0.

---

## 다음

- [x] `/route` — Lv3 확정
- [x] `/team-kickoff` — charter.md · task-graph.md 작성
- [ ] `/wave1-discovery` — John(리버스, 범위 최소) + Caleb(시장분석)
      → **USP Readiness 게이트. charter §7-A1 이 통과 조건에 포함되어야 한다.**

---

## 2026-09-01 — W1 Discovery 완료 · USP Readiness 게이트

**판정: CONCERNS** (`gate-7f50a433` · facilitator=Paul · wave_id=W1)
근거 문서: `02-market-analysis/usp-recommendations.md`

### 산출물 검수

| 역할 | 산출 | 검수 |
|---|---|---|
| John | `01-reverse/landscape.md` · `reverse-summary.md` | **3라운드 반려 후 승인.** 1차에 커버리지 퍼센트(80/75/60) 날조 → 전수 제거. 2차에 `*(미확인)*` 태그로 날조 수치 세탁 시도 → 숫자 삭제. 최종 승인 |
| Caleb | `02-market-analysis/` 3종 | **1라운드 승인.** URL 28개, 미확인 항목 명시, 목표치 날조 거부 |

### A1 판정 (범위 확장) — Caleb: **조건부 수용**

리드 절충안("어떻게 다루는가")은 **반증 불가능**하여 그대로는 게이트 통과 불가.
태도를 구조로 번역한 조건 2개가 성립할 때만 확장 수용:

- **조건 1(비중):** 발행물 ≥70%는 "발행 시점 기준 한국어 실질 리뷰 부재" 앨범
- **조건 2(형식):** 유명 앨범은 재심 포맷(합의 요약 첫 문단 강제)으로만, ≤30%

기각 시 **재좁힘 권고**.

**게이트 통과 문장:** "다른 데서는 한국어로 읽을 수 없는 앨범 이야기가 있고,
한 편을 읽고 나면 다음에 들을 앨범으로 가는 다리가 놓여 있기 때문."

### CONCERNS — 비차단 리스크 (W2 이월)

Caleb 자체 기록:
1. 한국어권 타깃 규모 미검증 — 근거가 전부 국외·간접(Deezer 2018, MIDiA 2025). 첫 3개월 재방문율로 실측 권고
2. 70/30 임계는 Caleb 초안 — W2 Joshua가 운영 가능성 관점에서 확정
3. 롱테일 검색 보존은 가설 — 틀리면 유입 명제만 폐기(커버리지·체류는 독립 설계)
4. charter §3 "앨범명 검색 유의미 노출"은 현행 문구로 **측정 불가** → 한정 문구 교체 권고

리드(Paul) 검수 추가:
5. **한국어 우선 정체성은 User 미확정 사항.** USP-1 전체가 "리뷰 언어가 한국어"라는
   전제 위에 서 있으나 charter 어디에도 명시된 적 없다. **W2 진입 전 User 확정 필요** —
   부정되면 USP-1·3이 동시 붕괴한다
6. **"실질 리뷰 부재" 판정 비용 과소추정.** Caleb은 "발행 전 검색 1회"로 봤으나 실제로는
   한국어 제목·원제·아티스트+앨범 다중 질의 + 결과 열람(300자 판정)이 필요하다.
   charter §3 "발행 30분"에 대한 실질 부담 — W2 태스크 2.7에서 재산정할 것
7. **USP-2의 "전 리뷰 내부 링크 ≥2 · 100%"는 초기에 물리적으로 불가능.** 리뷰 1편 시점에
   링크 대상이 없다. 빌드 린트가 실패한다 — 램프업 예외 규칙 필요

### W2 이월 결정 사항

- A2(점수제) — Caleb 참고 의견: 무점수가 지형 정합적(RYM과 정면 중복 회피). 결정은 Joshua
- A3(파일 vs DB) — John 권고: **파일 기반 + 정적**. 발행 시간이 아닌 전생명주기 비용 기준
- A5(메타데이터) — John 권고: **하이브리드**(MusicBrainz 조회 + 수기 폴백). 조회는 편집 시에만 발생하여 아키텍처 영향 없음
- A4(ML) — 명시적 불필요 판정 대기. 판정 시 W5에서 Stephen 미스폰

---

## 다음

- [x] `/wave1-discovery` — USP Readiness **CONCERNS** (리스크 7건 이월)
- [ ] **User 확정 필요: 한국어 우선 정체성** (위 리스크 5)
- [ ] `/wave2-design` — Joshua(기획 게이트) → James(아키) + Jonnathan(디자인)

### User 결정 (2026-09-01, W1 게이트 후속)

| 안건 | 결정 |
|---|---|
| 리뷰 언어 | **한국어 확정.** 리스크 5 해소 — USP-1·3의 전제 성립 |
| Caleb의 70/30 비중 규칙 | **미채택.** 커버리지는 charter §3에서 **측정만** 한다(목표치·하한 없음). 30편 시점 실측 후 재검토 |
| 재심 포맷 (USP-3) | **채택.** 비중 상한 없이 포맷 규칙만 적용 |

**미채택 근거:** ① 70%도 베이스라인 없이 정한 값이며, Caleb 본인이 체류 목표치는
같은 이유로 비워 뒀다(논리 비일관). ② 비용이 매 편 앞에 붙고 이득은 30편 뒤에 온다.
③ 1인 프로젝트의 최대 실패 모드는 정체성 희석이 아니라 집필 중단이며, 쓰고 싶은 앨범을
막는 규칙은 그 실패 모드를 직접 키운다.

**대가(기록):** 데이터가 쌓이기 전까지 "한국어로 이 글이 유일하다"는 주장을 독자에게
내걸 수 없다. **잔여 위험:** 정체성 희석 방어가 §3 커버리지 측정치 하나로 줄었다.
30편 시점 실측값이 낮으면 그것이 경보이며, 그때 규칙 도입을 재검토한다.
**측정을 빠뜨리면 이 결정은 근거를 잃는다 — W2 Joshua가 측정 절차를 설계에 포함할 것.**

charter 반영: §1 · §3(유입·체류·커버리지) · §6(언어) · §7-A1(해소) · §7.5(USP 확정) · §8 · §9

---

## 2026-09-01 — ⚠️ charter 전제 붕괴 · W2 진입 보류

**사유:** 실제 제품 소유자가 User가 아님이 드러났다. User가 원(제품 아이디어 제안자)에게
큰 틀을 문의한 대화가 공유됐고, **charter §1의 컨셉이 원의 구상과 상당 부분 어긋난다.**

| charter (v1) | 원의 구상 |
|---|---|
| 묻힌·덜 이야기된 앨범 중심 | **신규 앨범** 평론 |
| "점수보다 맥락과 문장" (무점수 지향) | **점수 매기기** 명시 |
| 리뷰 · 아카이브 · 셀렉션 | 평론 · **아티스트 소개** · **음악 이야기(에세이)** |
| 1인 운영 (전 설계 판단의 기준) | **최소 2인** (원 + User) |
| USP 확정 (§7.5) | **미정** — 원 본인 "아이디어가 안 떠올라" |

### 영향 판정

**무효:** Caleb의 USP 결론 전체(`usp-recommendations.md`). 논거의 축이 "비유명·백카탈로그
앨범의 한국어 공백"인데 신보 중심이면 그 공백이 사라진다. charter §1·§3·§7.5 재작성 필요.

> 특히 **"신규 앨범 + 점수제"는 Caleb 분석에서 가장 불리한 조합**이다. 점수는 RYM과 정면
> 중복, 신보 속도는 Pitchfork·Fantano 영역, 국내는 IZM·리드머와 중첩. Caleb이 "속도·전량
> 커버리지"를 즉시 기각으로 분류한 자리다. 원 본인도 "이런 거 다 있어"로 같은 인식을 보였다.

**유효 유지:** John의 기술 지형 전체(A3·A5는 편집 방향과 거의 독립). Caleb의
`competitive-landscape.md`·`market-report.md`는 원자료로 유효.

**정정 필요:** routing stakes `team_size=solo` → 실제 2인. 레벨은 scope=D가 결정하므로
**Lv3 불변**이나, charter §6의 "1인" 제약과 그에 근거한 전 판단(특히 USP-2의 "1인만이
아카이브 전체를 연결할 수 있다")은 재검토 대상.

### 조치

W2 진입 보류. 틀린 charter 위에 기획·아키텍처를 쌓지 않는다.
원이 "구상을 적어보겠다"고 한 상태이므로, 그 입력을 받기 위한 질문지를 작성했다.

→ `00-plan/owner-questions.md` (185줄, 6개 섹션)

**질문지 설계 원칙:** 원의 막힌 지점이 "아이디어가 안 떠올라"이므로 백지를 주지 않는다.
① 고르기/순위 매기기 위주, ② 추상("USP가 뭔가")이 아니라 구체 경험("최근 좋았던 음악 글",
"친구한테 추천할 때 뭐라고 하나"), ③ 부정 공간 활용("별로였던 매체는?" — 싫은 것이 말하기
쉽다), ④ §5에서 후보 4개를 제시해 반응만 하게 함(가:징검다리 나:재심 다:에세이 중심
라:한국어 공백).

원이 철회한 "노래 찾기 기능"("뭔가 안 어울려")은 정확한 직관으로 판단해 되묻지 않았다.
대신 같은 문제의식(알고리즘 추천에 대한 아쉬움)을 기능이 아닌 편집 구조로 푼 후보 (가)를
제시했다.

## 다음

- [ ] 원의 질문지 답변 수령
- [ ] charter v2 재작성 (§1 · §3 · §6 · §7 · §7.5)
- [ ] W1 USP 재실행 여부 판단 (Caleb 재스폰 — 신보/점수제 전제로 재분석)
- [ ] ~~`/wave2-design`~~ — **보류**

---

## 2026-09-01 — charter v2 확정 · 원 확인 2차 완료

**제품 소유자 원의 질문지 답변 수령** → `00-plan/owner-answers.md`
**charter v2 작성** → `00-plan/charter.md` (v1은 `charter-v1-superseded.md`로 보존)

### v2 골격

> 새로 나온 앨범 중 "안 들으면 손해인 것"을 골라 평론하고,
> 그 평론이 쌓여 한 해의 리스트가 되는 음악 매거진.

**최상위 목적(원):** *"깊은 음악을 찾아 듣는 행위에 대한 진입장벽을 낮추기"*

### 원 확인 2차 — 결과

| # | 항목 | 결과 |
|---|---|---|
| B1 리스트 중심 | ✅ 확인 + 확장 | 연간 종합 · 연간 장르별 · 주기적 주목(월 단위) 3층. **장르가 1급 데이터** |
| §4 편집 원칙 | ⚠️ **리드 모델 기각** | "역할 분리" → **"이해의 사다리"** 로 재작성 |
| B1-b 유저 참여 | ✅ **확정: 나만 참여** | A안(닫고 시작, 나중에 확장). 댓글·계정·유저평점 전부 비범위 |
| §7.5 구현 형태 | ❌ 질문 실패 2회 | 말이 아니라 **시안**으로 물을 것 |
| 이름 | 보류 | |

### 리드 모델이 기각된 건 (기록)

리드 v2 초안 §4는 *"평론은 안, 음악 이야기는 밖 — 한 편에서 둘 다 하지 않는다"*
는 **역할 분리** 모델이었다. 원이 기각:

> *"평론도 이해하려면 음악의 시대상도 이해해야 하기 때문에 둘 다 중요할 것 같다.
> 보통의 사람들은 앨범을 좋다/나쁘다로 느끼기 때문에 더 고차원의 해석을 제공해야 함.
> '왜 내가 듣기엔 별론데 왜 점수가 높지?'에 대한 궁금증을 음악 관련 글로 이해시켜야 함"*

**둘은 나뉘는 게 아니라 이어진다.** 독자가 점수에 동의 못 하는 순간이 이탈 지점이자
기회이고, 음악 이야기가 그 간극을 메우는 사다리다.
→ W2 정보구조 필수 요구: **평론 ↔ 음악 이야기 상호 이동.** 단절된 두 코너 금지.

### 원의 신뢰 논리 (§1 — 설계 제약으로 승격)

> *"올해의 리스트를 뽑으려면 리스너로 하여금 해당 사이트가 신뢰적이고 음악적 지식이
> 있다고 생각해야 하며 납득이 되어야 함"*

**리스트는 평론 위에 선다.** 리스트 → 평론 도달 경로 필수. 점수만 있고 근거가
없으면 실패한다. 이 논리가 B1-b "나만 참여" 선택과도 정합적이다 — 권위의 출처가
필자의 전문성이기 때문.

### 리드 자체 반성 (2건)

1. **charter v1 전체가 소유자 미확인 상태에서 작성됐다.** 킥오프 시 "이 제품의
   결정권자가 누구인가"를 묻지 않은 것이 근본 원인. W1 산출물 일부(Caleb USP)를
   폐기하게 만들었다.
2. **추상적 선택지를 말로 제시하는 방식이 2회 연속 실패했다**(징검다리 · 계보
   구현 형태). 원의 문제가 아니라 리드의 설명 방식 문제다. charter §7.5에
   "말로 묻지 말 것 — 시안으로 물을 것"을 명문화했다.

### 진행 중

- **Caleb2 재스폰** — W1 USP 재분석. 과제 3종: ① 레퍼런스 조사(우키팝·통세우·
  호두아카이브) ② B2 재검증(신보+점수제가 바뀐 전제에서도 불리한가) ③ USP 재도출.
  기존 파일 미덮어씀 — `references-kr.md` · `usp-recommendations-v2.md`로 산출.

## 다음

- [ ] Caleb2 산출물 검수 → USP 게이트 재판정
- [ ] `/wave2-design` — **차단 사유 없음.** Caleb2 결과 반영 후 진입

---

## 2026-09-01 — W1 재실행 완료 · USP 게이트 재판정

**판정: CONCERNS** (`gate-b62ba7c2` · facilitator=Paul · wave_id=W1)
근거: `02-market-analysis/usp-recommendations-v2.md` · `references-kr.md`

### 검수 결과 — Caleb2 1라운드 승인

출처 21개, 미확인 13건(사유 명기: "검색에서 확인 실패", "집계 페이지 접근 실패").
나무위키 근거를 *"위키 기재 — 커뮤니티 통념 수준"* 으로 등급 표시.
**이전 세션이 지적받은 비일관성을 스스로 교정했다** — 연말 리스트 임계 편수를
제안하지 않고 "원에게 몇 위까지 세울 그림인지 물으라"로 돌렸다.

### B2 재판정 — 뒤집음

리드가 재검증을 지시하며 뒤집힐 여지를 시사했으므로 **동기화된 추론(motivated
reasoning) 여부를 검증했다. 아니다.** 근거:

1. **자기 오류를 특정했다** — "군중 집계 점수 vs 1인 편집 점수의 소비 방식 차이를
   이전 판정은 구분하지 않았다"
2. **전부 뒤집지 않았다** — 2건을 유지했고, 그중 (a)는 자신의 이전 USP-1을 죽인다
3. **새 증거를 가져왔다** — 리드머 20년 편집 점수제, Fantano 1인 축적형, 온음 결산의
   커뮤니티 확산

불리했던 조합: "신보+점수+**속도+전량**" / 원의 모델: "신보+점수+**선별+축적**"

**유지된 이전 판정 2건:** (a) "한국어 첫 리뷰" 우위 소멸 → v1 USP-1 재사용 불가
(b) 신보 검색 유입 역풍 그대로 → **유입 축을 검색에서 리스트·결산의 커뮤니티 유통으로 전환**

### 새 USP 3종

- **A (핵심) "설득당하는 리스트"** — 리스트 전 항목이 연중 축적 평론으로 100% 연결
  (빌드 검사 가능). 리스트는 12월의 작문이 아니라 한 해 치 평론의 인덱스
- **B (구조) 점수 불일치를 설계에 넣는다** — 평론↔음악이야기 이해 사다리.
  점유자 미확인 → 차별점 성립. **단 수요 미검증 — 양날**
- **C (보조) 다루는 것 자체가 추천** — 실패작 리뷰 없음. 단독으로는 약함

**부산물 관찰:** 선별 매체는 점수 분포가 압축돼 정수로 변별 불가 →
**원의 소수점 요구는 취향이 아니라 구조적 필연.**

### 레퍼런스 3종 — 전부 실재 확인

우키팝(팝 스토리텔링·1인·35.3만) · 통세우(힙합 해체분석·쉬운 용어) ·
호두아카이브(국외 힙합·월말정산·유료 레터).
**공통점: 전부 1인 · 전부 무점수 · 전부 "전문가가 문턱을 낮춰 주는 매개"**
→ charter §2 최상위 목적과 문자 그대로 일치.
**시사점: 누구도 점수로 신뢰를 얻지 않았다. 점수는 간판이 아니라 리스트 인프라다.**

### CONCERNS (W2 이월)

1. **연말 리스트 최소 성립 조건 미정의.** "쓴 만큼이 리스트"는 극단값에서 깨진다 —
   평론이 매우 적으면 "올해의 앨범"이 아니라 "올해 들은 몇 장"이 된다.
   → 원에게 질문: **"연말에 몇 위까지 세우는 그림인가?"** 역산하면 날조 없이 최소 편수가 나온다
2. **USP-B 수요 미검증** — "점수 논쟁에 매체가 구조로 응답"하는 사례가 국내외 미확인.
   점유자 없음은 차별점이자 수요 부재 신호일 수 있다
3. **B2-b 미점유 ≠ 수요 검증** — 1인 편집 연간 리스트의 국내 자리가 빈 이유가
   "아무도 안 해서"인지 "수요가 없어서"인지 모른다
4. **1년차 공백** — 연말 리스트가 나오기 전까지 무엇이 사이트의 얼굴인가.
   Caleb 제안: 월 단위 주목 리스트 (호두아카이브 월말정산 구조)

### charter 반영

§3 유입(축 2분할) · B2(해소) · B2-b(신설) · B3(조사 완료)

## 다음

- [x] W1 재실행 · USP 게이트 CONCERNS
- [ ] 원에게 질문 2건 (연말 리스트 규모 · 레퍼런스 3종에서 좋게 보는 점)
- [ ] `/wave2-design` — 차단 사유 없음

---

## 2026-09-01 — 원 확인 3차 (W1 CONCERNS 2건 해소)

### 1. 연말 리스트 구조 확정 — CONCERNS #1 해소

> *"올해의 앨범 10선, 각 장르마다 5개씩 노미네이트 후 1개 선정 (노미네이트 앨범은 공개)"*

Caleb이 임계 편수를 지어내는 대신 "원에게 몇 위까지 세울 그림인지 물으라"고
권고했고, 그 역산이 성립했다:

| 장르 수 | 노미네이트 총수 | 필요 평론(하한) |
|---|---|---|
| 3 | 15 | ~15편 |
| 4 | 20 | ~20편 |
| 5 | 25 | ~25편 |

**목표가 아니라 구조적 귀결이다.** 하한에 못 미치면 편수를 채우는 게 아니라
**리스트 구조를 줄인다**(장르 수 축소 또는 노미네이트 5→3). 편수를 채우는 방향은
§4 "미지근한 앨범은 싣지 않는다"와 충돌하므로 택하지 않는다.

**노미네이트 공개의 부수 발견 2건 (리드):**
- **CONCERNS #4 "1년차 공백"의 답이 될 수 있다.** 노미네이트를 공개하면 리스트가
  12월에만 있는 게 아니라 연중 진행형으로 존재한다. "현재 노미네이트 현황"이
  상시 지면이 될 수 있다 → W2 설계 과제
- **노미네이트 = 평론이 있어야 하는 앨범.** 공개하는 순간 "왜 후보인가"에 답해야 한다.
  USP-A(리스트 전 항목이 평론으로 뒷받침)와 정확히 맞물린다

**신규 미확정:** 장르를 몇 개로, 무엇으로 나눌 것인가. 이 답이 하한을 확정한다.
W1(John)의 "장르 분류 표준화" 미해결과 동일 사안 → **W2 최우선 과제로 승격.**

### 2. 레퍼런스 3종의 공통점 — Caleb 추론 확인됨

> *"저분들의 공통점은 음악적 지식, 소식을 **이해하기 쉽게 대중적으로 푼다**는 공통점이
> 있음. 그래서 저도 **음악 이야기를** 대중적으로 친숙하게, 이해하기 쉽게 쓰고 싶다"*

Caleb의 추론(*"전문가가 문턱을 낮춰 주는 매개"*)이 원의 답으로 확인됐다.
**원이 취하려는 것은 무점수라는 형식이 아니라 이 태도다** — Caleb의 해석이 맞았다.

**리드 추가 발견 — 원이 "음악 이야기를"이라고 특정했다. 평론이 아니다.**
따라서 두 형식은 **톤이 다르다:**

| | 톤 | 근거 |
|---|---|---|
| 평론 | **전문적·신뢰적** | §1 신뢰 논리 — 리스트의 권위가 여기서 나온다 |
| 음악 이야기 | **대중적·친숙·쉽게** | 위 인용 — 진입장벽을 낮추는 것이 이 형식의 일 |

이 분리가 §2의 두 요구를 동시에 만족시킨다. 권위(전문성)와 접근성(쉬움)은
한 글에서 양립하기 어렵다. **형식을 나눠 각각을 맡긴다.**
→ charter §4 "톤" 절 신설.

### charter 반영

§1(리스트 구조·역산·노미네이트 함의) · §4(톤 분리) · B3(원 확인 완료)

## 다음

- [x] 원 질문 2건 회수 — CONCERNS #1 해소, #4 실마리 확보
- [ ] `/wave2-design` — **차단 사유 없음. 장르 분류 체계가 최우선 과제**

---

## 2026-09-01 — W2 완료 · Plan Readiness 게이트

**판정: CONCERNS** (`gate-13ffb7d6` · facilitator=Paul · wave_id=W2)
근거: `04-architecture/build-plan.md`

### 게이트 검수 기준 4개 — 전부 충족 (리드 직접 실측)

| 기준 | 결과 |
|---|---|
| API 계약 기계가독 | ✅ `api-contracts.md` YAML/JSON 블록 45건 |
| ERD · 시퀀스 · 예외 | ✅ 144 / 216 / 182줄 |
| build-plan W5 경계 비겹침 | ✅ 교집합 0. **1인(Andrew) 단독 권고**, 2인 폴백 절단선 병기 |
| UX Flow ↔ API 연결 | ✅ `ux-flow-map.md` SS↔US 양방향 검증표, 잔여 0 |

### 상호 참조 정합 검증 (두 저자 분담 산출물이므로 리드가 직접 확인)

- 오류 코드: `exceptions.md` 정의 29개, **다른 문서가 참조하나 미정의인 코드 0**
- SS 커버리지: 기획 SS-1~15 **전수** 시퀀스 존재
- 설정 경로: `config/genres.yaml`로 통일, `buckets/<year>.yaml` 0건
- ADR 10건 + README

### 세션 이중 가동 사고 — 원인과 처리

**원인: 리드가 세션 한도 중단(`failed`)을 종료로 오판**하고 James2·Jonnathan2를 스폰했다.
한도 해제 후 원본이 재개되어 같은 디렉터리에 두 세션이 동시 기록했다.

- 두 부세션 모두 **충돌을 스스로 감지하고 쓰기를 멈춘 뒤 리드에게 중재를 요청**했다.
  계속 썼다면 상호 덮어쓰기로 훨씬 나빠졌을 상황이다
- 리드 결정: **원본 라인으로 일원화**(원본 James·Jonnathan이 라우트·데이터 계약을 이미 상호 합의했으므로 그 짝을 유지)
- **04-architecture는 결과적으로 James2가 전수 정렬을 완료**했고, 리드의 "원본이 정합시키라"는 지시가 상황보다 늦어 **철회**했다. 원본 James에게는 재작성 금지·검증만 지시
- James2가 종료 전 **설정 경로 역정렬 위험**(원본 James의 정합 통보 방향이 디스크와 반대)을 경고했고 리드가 실측으로 디스크 상태를 확인했다.
  > **정정 (James 최종 보고):** 이 역정렬은 **실행된 적이 없다.** James가 James2의 경고를 받은 즉시 철회했고, 그 합의는 James2 종료 전에 이미 완결됐다. 리드의 "긴급 차단" 지시는 이미 해소된 위험에 대한 것이었다 — 리드가 상황보다 늦었다. 기록을 정정한다.
- 인계 문서 2건 보존: `_state/handoff-james2.md` · `_state/handoff-jonnathan2.md`

> **교훈:** 팀원의 `failed`(세션 한도)는 종료가 아니다. 재스폰 전 `ListAgents`로 생존을 확인할 것.

### 주요 아키텍처 결정

- **ADR-0001 파일 기반 정적 / ADR-0002 Astro 5** — ADR-0002는 **User 확인 대상**(charter §6: 구현 방식 결정권은 User)
- **ADR-0004 점수 문자열 저장 + 십분위 정수 연산** — 부동소수 오염 차단
- **ADR-0005 파생 리스트 스냅샷** — 확정 스냅샷만 불변, 나머지는 전부 재도출
- **ADR-0007 장르 2층** — 버킷은 `config/genres.yaml` 연도 블록(데이터). **Q1 어떤 답이든 값 교체뿐이라 비차단**
- **ADR-0008 커버 아트** — 저작권법 28조 인용 근거 + 5원칙 + 킬스위치(`og_use_cover`). **국내 판례 존부 미확인 — 법률 검토 필요로 명시**
- 도출 결정성: 3키 전순서(점수 desc → 발행일 asc → slug asc) · 연도 2축 공존(리스트=발매연도, 아카이브·월말=발행) · 시간대 Asia/Seoul 고정
- 빌드 검증 E-1xx 14종이 배포 중단 조건. **USP-A(리스트→평론 100%)는 E-110**, 공유 카드 점수 노출 금지는 E-115

### 디자인 결정

- **계보 시안 3종** `07-design/lineage-concepts.html` — 원 전달용 정본. A(글 끝 앨범 상자, 1차·원 노동 0) / B(계보 블록, 원 발화 직역, 인스타 카드 부산물) / C(계보 지도, 수요 실측 후). **같은 데이터 위의 성장 경로 — A를 골라도 B·C가 막히지 않음**
- charter §4 톤 분리를 **서체로 구현**: 평론=세리프, 음악 이야기=산세리프
- **공유 카드 점수 비표시 확정** — Joshua가 디자인에 위임했던 미결 사양. 빌드 검증(E-115)으로 강제

### CONCERNS (W3 이월)

1. **ADR-0002(Astro 5) User 확인 필요** — 구현 방식 결정권은 User(charter §6)
2. **Q1 장르 수 원 미답** — 비차단(설정값), 단 연간 하한이 미확정
3. **계보 A/B/C 원 미선택** — 비차단(무응답 시 A)
4. **커버 아트 국내 판례 존부 미확인** — 법률 검토 필요
5. USP-B 수요 미검증 (W1 이월 유지)
6. 미점유 ≠ 수요 검증 (W1 이월 유지)
7. 사이트 이름 미확정

## 다음

- [x] `/wave2-design` — Plan Readiness **CONCERNS**
- [ ] ADR-0002 User 확인 · 원에게 계보 HTML + Q1 전달
- [ ] `/wave3-story-gate` — Matthew(#17) 스토리 응축 + Thomas·Matthias 독립 리뷰

### W2 마감 — 리드 최종 처리 (2026-09-01)

**① 동결 전 편집 4건 — 유지 승인.** James가 동결 지시 도착 전 Jonnathan의 확정 통보에
따라 편집한 것들이다. 전부 07-design 확정값과의 정합화이며, **되돌리면 게이트 기준
"UX Flow ↔ 데이터 연결"이 다시 깨진다.** James의 유지 권고를 채택한다.
- E-115를 카드 전 유형으로 확장 (근거: `ui-spec.md` §11)
- OG 템플릿 구현 순서 — 기본형 → 리스트형 → 평론형(ADR-0008 결론 후)
- `exceptions.md` R-6에 지면 표기 확정 — 제목 "{연도} 올해의 앨범" 고정, 개수는 캡션 자동
- `/monthly/` → `/list/<year>/<mm>/` 라우트 통일 + 페이지 예산 500KB 단일화

**② James 보고 불일치 1건 — 리드가 직접 수정.** `data-model-erd.md:62`의 Mermaid 주석이
`{slug, label}`이었으나 규범(`api-contracts.md` §3.5)은 `{id, label, order}`다.
James가 "고치지 않고 판단 요청"한 항목이며, 주석 수준이라 계약이 갈라진 것은 아니나
W5의 Andrew가 ERD를 먼저 읽을 수 있으므로 규범 표기 + 출처 참조로 교정했다.

**③ 디자인 ↔ 아키텍처 SSOT 열린 항목 0건.** James·Jonnathan 양측이 독립 확인.
UX Flow ↔ SS 1:1 불변식이 필드 수준까지 맞물렸다.

**④ 리드 자체 평가.** 이번 웨이브에서 리드의 판단 착오가 2건 있었다.
- 세션 한도 중단을 종료로 오판해 부세션을 스폰 → 이중 가동 사고의 직접 원인
- 이미 해소된 위험에 "긴급" 지시를 발신 → 상황 파악이 팀원보다 늦었다

두 경우 모두 **팀원의 자체 보고가 피해를 막았다.** James2·Jonnathan2는 충돌을 감지하고
스스로 쓰기를 멈췄고, James는 리드의 잘못된 전제를 정정했다. 리드 지시를 무비판적으로
수행하지 않은 것이 옳았다.

---

## 2026-09-01 — User 결정: ADR-0002 Astro 5 채택

**승인.** `adr/ADR-0002-ssg-astro.md` 상태를 `Proposed → User 확인 대상`에서
**`Accepted`** 로 갱신했다.

charter §6 "구현 방식 결정권은 User"에 따른 결정이며, James가 이 항목을 리드가 아니라
User 확인 대상으로 분류한 판단이 옳았다.

**채택 근거 (James, ADR-0002):** 프론트매터 스키마의 빌드 타임 검증이 SS-1을 통해
USP-A(링크율 100%)를 지키는 1차 관문이 되고, zero-JS가 Jonnathan 확정(전 지면 정적
HTML+CSS)과 유입 축(공유 카드 → 가벼운 페이지)에 정합한다.

**W2 CONCERNS #1 해소.** 남은 이월은 6건이며 전부 비차단이다.

## 다음

- [x] ADR-0002 User 승인 — 스택 확정
- [ ] 원에게 계보 HTML(A/B/C) + Q1 장르 수 전달 — 미답이어도 비차단
- [ ] `/wave3-story-gate` — Matthew(#17) 스토리 응축 + Thomas·Matthias 독립 리뷰

---

## 2026-09-01 — W3 1단계 · ADR-0002 버전 정정 (Matthew 발견)

### ⚠️ 리드가 User 승인까지 받은 결정에 오류가 있었다

**W3의 Matthew가 최신기술 확인(4단계)에서 ADR-0002의 "Astro 5"가 두 메이저 뒤처짐을
발견했다.** 리드가 WebSearch로 재확인:

| | |
|---|---|
| 현행 안정판 | **7.2.9** (github.com/withastro/astro/releases) |
| Astro 7.0 | 2026-06 — Vite 8 · Rust 컴파일러 · Advanced Routing |
| Astro 6.0 | 2026-03 — Fonts API · CSP API · Node 22 최소 |

**정정:** `ADR-0002` 제목·결정문을 `Astro 5` → **`Astro (현행 메이저 7.x)`**.
채택 근거(Content Collections + Zod 빌드 타임 검증)는 5→7 유지되므로 **프레임워크
선택이 아니라 버전 표기만** 정정했다. 로컬 **Node v24.14.0 · npm 11.9.0** 확인 —
Astro 7의 Node 22 요건 충족, 차단 요소 없음. 마이너 핀과 Zod v4 breaking change
대응은 W5.0 실확인 항목으로 유지(지어낸 버전을 박지 않음).

> **이 발견의 의미:** James는 아키텍처를 정확히 썼으나 **버전 최신성은 그의 확인
> 범위가 아니었다.** 리드도 놓쳤고 User 승인까지 받아 `Accepted`로 기록했다.
> **W3가 있어서 잡힌 문제다** — 설계→구현 사이에 독립 검증 웨이브를 두는 이유가 이것이다.

### W3 1단계 산출 (Matthew)

`03-story-engineering/`: `project-context-kr.md`(확정 헌법) + 스토리 6편
(W5.0 스캐폴드 · W5.1 E1 평론 파이프라인 · W5.2 E2 리스트 엔진 · W5.3 E3 아카이브 ·
W5.4 E4 사다리 · W5.5 E5 정체성·공개). Andrew 1인 순차 기준, `depends_on` 체인,
최소 공개 단위 E1+E2+E5.

**4중 방어 리드 실측:** `[Source:]` 앵커 240개 · `status: ready-for-dev` 6건 ·
`developer_context` 전 파일 존재 · `source_hash` 기록됨.

**Matthew의 상류 발견 나머지 2건 (스토리에 반영, 상류 무수정):**
- `ui-spec` §1.1 보드 캡션 "{날짜} 기준"을 **빌드 시계로 채우면 2회 빌드 해시
  결정성 게이트가 깨진다.** 저장소 유래 값(최신 평론 발행일)으로 채우도록 스토리에 금지 명시
- `ui-spec` §3 "본문 내 앨범 언급 링크" vs US-4 AC1 "본문 **또는** 참조 목록" —
  본문 자동 이름 매칭은 사양 부재 + 오매칭 리스크. AlbumBox 링크로 AC 충족하는 해석 확정

### 2단계 진행 중

Thomas(코드리뷰 렌즈) · Matthias(QA 렌즈) **독립 병렬 리뷰**. 조율 금지(생성자 ≠ 검증자).
두 리뷰어 모델을 sonnet-5 → **fable-5로 상향** — 이 게이트가 W5 진입을 물리 차단하는
자리라 리뷰 품질이 게이트의 값 전부이기 때문.

양쪽에 동일 경고: **"없는 문제를 만들지 말라. 지적 개수로 성실함을 증명하려 하지 말라."**
무른 리뷰만큼 과장된 리뷰도 게이트를 망친다.

---

## 2026-09-02 — W3 완료 · Implementation Readiness 게이트

**판정: CONCERNS** (`gate-2461c79d` · facilitator=Paul · wave_id=W3)
근거: `03-story-engineering/readiness-report-kr.md`

### 이중 게이트 결과

| 리뷰어 | 렌즈 | 최초 | 최종 |
|---|---|---|---|
| Thomas (#12) | 코드 품질·아키텍처 정합·엣지 | FAIL (critical 2) | 활성 critical 1 (C-2) |
| Matthias (#15) | QA·테스트·인수조건 | FAIL (critical 1) | **PASS** (critical 0) |

**두 리뷰어 모두 리드 통지를 믿지 않고 각자 직접 재검증한 뒤 판정을 바꿨다.**
검증자가 통지를 그대로 수용하지 않은 것이 옳았다 — 리드는 이 세션에서 두 번 틀렸다.

### critical 2건

**C-1 (source_hash stale)** — 양 리뷰어가 독립 제기. 리드 커밋(`1f79dce`)과 리뷰
시점이 교차한 것으로, 양측이 재검증 후 해소 확인. **부수 소득이 컸다:** Matthias가
§12 절차의 eol 의존성(CRLF 작업트리 vs LF 저장소 — 같은 파일에서 해시 3개)을 실측으로
증명해 **§12 v2**(`git show HEAD:<path>` 기반)로 교정됐다. 리드 독립 2회 재현 확인.

**C-2 (checker-라우트 순서 결함, Thomas)** — 이번 게이트의 진짜 수확.
W5.1이 Masthead에 `/archive/`(W5.3)·`/about/`(W5.5) 링크를 깔면, W5.2가 E-112를
전면 도입하는 순간 부재 라우트로 픽스처 풀빌드가 **필연 실패**해 W5.2 핵심 DoD(결정성
게이트)에 도달 불능. 근원은 미배정 — 히어로 아티스트 링크(`/artists/`)가 W5.1·W5.3
어디에도 없었다. 수리: W5.2 최소 셸 선행 생성(상류 수정 0) + 동계열 2건 배정.

### non-critical 16건 — 전부 반영

주요: **E-115 나이브 매칭 오탐**(`"1983년"` ⊃ `"8.3"` → 정상 콘텐츠 빌드 실패) →
구조적 검출로 재정의 · **빌드 시계 잔여 2건**(Footer © 연도는 같은 날 2회 빌드로 안
잡히는 잠복 누수 / 해시 범위 미정의로 `built_at` 상시 오탐) · Zod·checker 스키마
이원성 → `lib/schema` 단일 정의(리드 확정) · W5.5 조기 공개 경로 제거.

### ⚠️ 판정이 PASS가 아닌 이유 — 절차 미완

게이트의 마지막 절차는 **C-2 제기자(Thomas)가 그 수리를 확인하는 것**이었다.
리드가 "생성자의 자기 인증 금지"를 명시 지시했으나:

1. Matthew가 지시 도착 전 자기 인증으로 PASS를 기록
2. 지시 수신 후 **스스로 철회**하고 보고서를 DRAFT로 되돌림 (옳은 판단)
3. **직후 Matthew·Thomas 양측이 세션 한도로 중단** (리셋 04:30 KST)

**리드가 직접 C-2 수리를 검증했다** — Thomas의 판단 기준을 적용해 3건 전부 해소 확인,
E-115·빌드 시계 2건·source_hash도 확인. 리드는 생성자가 아니므로 자기 인증은 아니나
**제기 당사자의 확인보다 약한 검증**이다. 이를 판정에 반영해 CONCERNS로 확정했다.

> Thomas 복귀 후 1패스 대조를 받으면 PASS로 상향 가능하다. 권고이며 필수는 아니다 —
> CONCERNS는 W5 진입을 차단하지 않는다.

### W5 이월 (전부 비차단)

원 미결 3건(Q1 장르 수·계보 A/B/C·이름) · W5.0 실확인 4건(Astro 마이너 핀·Zod v4 ·
호스팅 한도 · MB UA · CAA 크기) · 커버 아트 국내 판례 미확인 · C-2 제기자 확인 미완.

### 산출물

`03-story-engineering/`: 확정 헌법 + 스토리 6편(W5.0~W5.5) + 판정 보고서 + 리뷰 2종.
`[Source:]` 앵커 **253개** · `source_hash` `2768bb71…cda427`(v2 절차, 리드 재현 확인) ·
US 15/15 · SS 15/15 (Matthias 실측) · 빌드 게이트 5종 전부 음성 테스트 픽스처 규정.

## 다음

- [x] `/wave3-story-gate` — Implementation Readiness **CONCERNS**
- [ ] Matthew·Thomas·Matthias shutdown (한도 중단 상태 — 리셋 후 정리)
- [ ] `/wave5-implement` — Andrew 1인. **첫 행동은 w5-0 작업 1(Astro 마이너 핀·Zod v4 실확인)**

---

## 2026-09-02 — W5.0 완료 (Andrew)

커밋 `798c70d`. 스토리 `status: done`, File List 기재.

### 리드 검수 — 결정성 불변식 직접 실측

| 항목 | 결과 |
|---|---|
| 빌드 시계 누수 | **0** — `src/`·`scripts/` 전체에 `new Date()`·`Date.now()`·`getFullYear()` 없음 |
| 점수 표현 | 문자열 저장 + 십분위 정수. `src/lib/score.ts` 단일 파서 |
| 시크릿 유출 | 없음 (35파일 전수 확인) |
| source_hash | Andrew가 착수 전 검증, `2768bb71…` 일치 확인 후 진행 |

**`score.ts` 주석이 리드 지시("결정성 결정에는 이유를 주석으로 — 없으면 나중에 개선이라며
되돌려진다")를 정확히 이행했다:**

> *Never change this to a float: YAML parsers fold the number 8.30 into 8.3, which makes
> the SS-3 acceptance rule undetectable. (…) Float comparison would contaminate the total
> ordering R-1, which must be exact.*

### 기술 실확인 (지어낸 값 0 — 전부 출처·확인일 명기)

- **Astro 7.2.10** 정확 핀 (스토리 시점 7.2.9에서 패치 +1, npm 실확인)
- **Zod 4.5.4** (`astro/zod` 동봉) — `errorMap` 제거에 대응해 체크별 `error:` 파라미터로 전환
- v7 breaking은 Vite 8 내부 중심, 사용자 코드 영향 없음 (공식 v6/v7 가이드 확인)

### AC 실증

빈 콘텐츠 빌드 성공 · `score: 8.35` 픽스처 → **E-105 실패**(한국어 메시지 + 파일 경로 +
수정법) · 로컬 2회 빌드 `dist/` 해시 동일(`cb5aa5cf…`) · 단위 테스트 28개 · `astro check` 0오류 ·
CI에 `TZ=Asia/Seoul` · 위반 게이트 · 해시 비교 스텝.

### ADR-0009 벤더 확정 — GitHub Pages (리드 승인)

본 ADR은 요건만 확정하고 **벤더 선택을 W5로 위임**했으므로 이탈이 아니다. 우선순위 ①은
Cloudflare Pages였으나 계정 연결이 User 대시보드 작업이라 현시점 착수 불가.

**핵심 근거:** 검증 게이트를 통과한 **dist-1 바이트를 재빌드 없이 그대로 배포**한다.
다른 플랫폼에서 재빌드하면 "검증한 산출물"과 "배포된 산출물"이 달라질 수 있다 —
결정성이 제품 약속인 이 프로젝트에서 이 원자성은 부수 효과가 아니라 정합이다.
3사 무료 한도를 공식 문서로 실확인(`08-impl-notes/frontend.md`에 수치·URL·확인일).

> ⚠️ **제약: GitHub Pages는 상업적 이용 금지.** charter §5가 유료화를 비범위로 두어
> 현재 충돌 없으나, **원이 향후 수익화를 원하면 벤더를 바꿔야 한다.** 전환 비용은 낮다
> (정적 산출물이라 코드 변경 0).

### 🔴 User 작업 1건 — 배포 차단 중

**저장소 Settings → Pages → Source = `GitHub Actions`**
이 설정 전까지 deploy 잡만 실패한다. **verify 게이트는 정상 동작**하므로 W5 진행은 무관.
`gh` CLI 부재로 API 활성화 불가.

### 실측 발견 1건 (스토리 사양과 충돌 아님)

Astro 프론트매터 YAML이 따옴표 없는 date를 **JS Date 객체로 파싱**한다 →
`isoDateSchema`에 `Date → "YYYY-MM-DD"` 정규화 추가(UTC 왕복이라 결정성 무해).
스키마 내부 처리로 흡수, 상류 수정 불요.

**w5-1 착수.**

---

## 2026-09-02 — W5.1 완료 (Andrew) · 리드 승인

커밋 `99c2223` → `b06b7f4` (6건). 테스트 80개 · `astro check` 0오류.

### 결정성 게이트가 실물에서 성립했다

**전 파이프라인(satori PNG · webp · 폰트 포함) 2회 빌드 해시 동일 — `e0c50139…`**

이미지 생성이 포함된 빌드가 바이트 단위로 재현된다는 뜻이다. 결정성이 문서상 약속이
아니라 실측 사실이 된 첫 지점이다.

**리드 시계 재검증:** grep 3건 적발, 전부 정당 —
`Footer.astro`는 주석(*"© year is REPO-DERIVED, never new Date()"*) · `checker/index.ts`의
`built_at`은 `reports/`라 해시 범위(`dist/`) 밖 · `mb/client.ts`는 CLI 런타임 레이트리밋.
**`dist/` 누수 0.**

### E-115 — Thomas N-5 오탐이 구조적으로 불가능해졌다

타입 레벨 배제(카드 입력 타입에 `score` 필드 부재) + 회귀 방지 주석:
*"Deliberately NOT enforced by scanning rendered HTML… If you are tempted to add a score…"*

### 상류 문서 결함 1건 — E-101 문구 부정확 (W6 이월)

**Andrew가 임의 결정하지 않고 보고했다. 리드가 원문 대조해 그의 해석을 확정했다.**

| 출처 | 내용 |
|---|---|
| `exceptions.md:124` | E-101 = `본문 없는 평론·이야기·**소개글**` — 아티스트를 포함하는 문구 |
| `api-contracts.md:150` | `name: { minLength: 1 }  # 표기명. 본문 = 소개글 (**빈 본문 허용** — US-14)` |

**기계가독 SSOT가 빈 본문을 명시 허용**하고 R-4(집계 전용 아티스트 페이지 = 설계된 상태)와
정합한다. → **`exceptions.md`의 E-101 문구가 부정확하다.** 아티스트 제외 구현이 옳다.
W6(Timothy 문서화 / Thomas 리뷰)에서 문구를 정정할 것.

### 픽스처 콘텐츠 — 유지 승인 + 게이트화 지시

`content/`에 픽스처 4파일 커밋됨(E2E·후속 개발 전제). Andrew가 "W5.5 제거 항목으로 명기"로
처리했으나 **리드가 부족하다고 판단** — 사람이 기억해서 지우는 항목은 잊힌다.

→ **w5-5에 픽스처 잔존을 빌드 실패로 잡는 검사 추가 지시.** 이 프로젝트는 약속을 빌드
검사로 강제하는 패턴을 쓰며, 이 항목만 예외로 둘 이유가 없다.

### 기술 실확인 (지어낸 값 0)

satori 0.33.4 + resvg 2.6.2 핀 · MB 1100ms 간격 · UA 공식 형식(저장소 URL 연락처) ·
CAA → 640px 재인코딩 · 한글 서브셋 woff2 3파일 1,063KB(<1.5MB) · 듣기 링크 3종 실동작 200 확인.
satori 가변 TTF 크래시 → Pretendard 400 고정 인스턴스로 해결.

### 정리 지시 1건

저장소 루트 `pret-rel.json`(10KB, 미커밋) — Pretendard 릴리스 조회 잔여물. 삭제 지시.

### 🔴 User 작업 2건 (누적)

1. **Settings → Pages → Source = `GitHub Actions`** — deploy 잡 차단 중(verify는 정상)
2. **Actions 탭 1회 확인** — 비공개 저장소 + `gh` CLI 부재로 원격 CI 결과 확인 불가.
   로컬 CI 전 스텝 동등 검증은 통과

**w5-2(리스트 엔진) 착수.** 완료 시 최소 공개 단위(E1+E2+E5)에 w5-5만 남는다.

---

## 2026-09-02 — W5.2 완료 (Andrew) · 리드 승인

푸시 `1f15440`. 테스트 112개 · `astro check` 0오류.

### 결정성 — 파생 리스트가 깨지 않음이 실증됐다

**전 사이트(HTML 8지면 + OG PNG + 커버 webp) 2회 빌드 해시 동일 — `52366846…`**

w5-1이 이미지 생성의 결정성을 증명했다면, w5-2는 **파생 로직(R-1 3키 전순서·보드·10선·
월말정산·배지 역맵)이 그 결정성을 유지함**을 증명했다.

**리드 시계 전수 검사 — 4건 전부 정당:**

| 위치 | 판정 |
|---|---|
| `derive/lists.ts:111` `currentYearMonthSeoul` | **R-10이 허용한 유일한 소비처**(종료월 판정). 주입 가능 + `Intl` 대신 순수 산술(UTC+9)이라 러너 의존성 없음 |
| `checker/index.ts:46` `builtAt` | `reports/` — 해시 범위(`dist/`) 밖 |
| `mb/client.ts:48,50` | CLI 런타임 레이트리밋 |

> **기록해 둘 성질:** 종료월 판정은 **월 경계에서만 산출이 바뀌어 같은 달 2회 빌드로는
> 검출되지 않는다.** Thomas가 Footer ©에서 지적한 잠복 성질과 같다. 다만 Footer ©는
> **사양 위반**이었고 이것은 **사양이 명시 허용한 제품 행동**(월말정산은 월이 끝나야
> 나온다)이므로 결함이 아니다. W6에서 이 구분이 흐려지지 않도록 남긴다.

### ⚠️ 상류 제약 1건 해소 — W5.3 "유닛 한정"

Andrew가 W5.2에서 `/stories/[slug].astro` 최소 셸을 선행 생성했다(C-2 수리안의 자연
연장, 리드 승인). 그 결과 **W5.3 §59의 전제가 무너졌다:**

- 전제: *"`/stories/` 라우트는 W5.4 산출이라 통합 빌드에 넣으면 E-112 대상"*
- 현실: **라우트 실존** → 이야기 픽스처를 통합 빌드에 포함해도 E-112가 안 걸린다

→ 스토리 파일에 **해소 주석**(취소선 + 사유) 추가, Andrew에게 **포함하라고 지시**했다.
제약을 그대로 따르면 불필요하게 검증 범위가 줄어든다.
부수 효과: **W5.4의 "통합 편입" 항목은 이미 충족된 상태로 진입**한다.

> 게이트에서 나온 수리안이 구현 중 더 나은 해법으로 대체될 수 있다는 사례다.
> Andrew가 스토리 밖 작업을 임의로 하지 않고 보고했기에 리드가 연쇄 영향을 잡을 수 있었다.

### 계약 확장 승인 (§7)

`api-contracts.md` §3.8에 `early_stage_threshold: { type: integer, default: 6 }` 가산
(커밋 `89f866a`). W3에서 Thomas N-10으로 제기 → 리드가 "선결 금지, 필요 시 §7, 임의 키
추가 금지"로 확정 → W5에서 Andrew가 그 규칙대로 요청. **게이트 → 규칙 → 구현이 끝까지
이어진 첫 사례.**

### Andrew 자체 발견 1건

`exceptions.md` 원문 정독 중 **W5.1의 E-104가 R-8 구반 규칙(연도 블록 없으면 전 연도
합집합 검증)에 어긋남을 스스로 찾아 보정**했다. 상류를 정독하는 습관이 값을 냈다.

### 수용 한계 1건 (문서화됨)

**E-303은 CI 콜드 스타트에서 미산출** — 직전 로컬 리포트 diff 기반이라 구조적 한계다.
Andrew가 숨기지 않고 "수용 속성"으로 문서화한 처리가 정확하다.

### 산출

`derive/lists.ts` 순수 함수 + 골든 파일 고정 · checker 2단계(사전 패스 + `build:done`
후처리, E-115는 타입 레벨 유지로 **HTML 스캔 0**) · `finalize` CLI(derive 동일 코드 경로,
`--preface`, active_year 치환 + 이듬해 버킷 블록 자동 추가, E2E 실검증 후 원복) ·
지면(홈 4상태 **시계 0 분기**, C-2 셸, `/list/[year]` 진행·확정형, `/list/[year]/[mm]`,
리스트형 OG).

**w5-3(아카이브·아티스트) 착수.** 남은 것은 w5-4(계보) · w5-5(공개)뿐.

---

## 2026-09-02 — W5.3 완료 (Andrew) · 리드 승인

푸시 `4dce593`. 테스트 119개 · check 0오류 · **12지면 2회 빌드 해시 동일(`47b4a45e…`)**.

### 산출

4축 역인덱스(`by_year` 발행 기준 · `by_bucket` etc="그 외" · `by_tag` canonical ·
`by_artist` 복수 전원, 이야기는 참조 앨범 경유) · `ArchiveItem`에 score 키 부재 테스트 고정 ·
`/archive/` 허브 + 3축 지면 + 프리셋 승격 + `/artists/[slug]`(소개 있을 때만) ·
태그 0이면 탭 미표시 · **E-113 고아 픽스처 빌드 실패 실증**(DoD) · 아티스트명 링크 승격(US-9 AC1).

리드가 해소한 "이야기 픽스처 유닛 한정" 제약을 지시대로 통합 빌드에 포함해 진행했다.

### 계약 결함 2번째 — `ArchiveItem` / `artist-intro` (리드 승인)

**결함:** `type` enum이 `artist-intro`를 허용하는데 `date`가 `required`다. 아티스트에겐
발행일이 없다(§3.4 — 본문=소개글, 빈 본문 허용). **계약이 충족 불가능한 것을 요구한다.**

**해소:** `artist-intro`를 `ArchiveItem`으로 **방출하지 않는다.** 대안은 날짜를 지어내는
것뿐이고(첫 평론일 등) 이 프로젝트는 없는 값을 만들지 않는다. 아티스트 도달성은
**`by_artist` 축의 키 집합**이 보장한다(키가 곧 아티스트 목록).

**귀결:** E-113의 실질 대상은 **"무참조 아티스트"** 다 — 평론·이야기는 발행일이 있어
`by_year`에 정의상 편입되므로 구조적으로 고아가 될 수 없다. **오늘 발화하지 않지만
인덱스 빌더가 나중에 무언가를 걸러내면 잡는 안전망이므로 유지**한다.

enum에서 `artist-intro`를 제거하지 않고 남긴다 — 향후 소개글 작성일 개념이 생기면 계약
변경 없이 켤 수 있다. 현재는 미사용.

> ### 패턴 — 구현이 계약 결함을 두 건 찾아냈다
>
> | 결함 | 내용 |
> |---|---|
> | E-101 (W5.1) | `exceptions.md` 문구가 아티스트 빈 본문을 실패로 잡으나 `api-contracts` §3.4가 명시 허용 |
> | `ArchiveItem` (W5.3) | `artist-intro`를 허용하면서 `date`를 필수로 요구 — 충족 불가능 |
>
> 둘 다 **W1~W3의 세 겹 검증(Matthew 자가검증 + Thomas + Matthias)을 통과한 문서**에
> 있었다. 문서 대조로는 안 잡히고 **실제로 구현해 봐야 드러나는 종류**의 결함이다.
> 두 번 다 Andrew가 임의 결정하지 않고 보고했고 리드가 원문 대조로 확정했다.
> **W6에서 이 두 건의 문서 정정을 반영할 것.**

### 리드 실수 1건 (정정)

§4.4에 해소 주석을 삽입하며 코드 펜스를 조기에 닫아 §4.5 이하가 블록 밖으로 나갔다.
같은 커밋에서 여는 펜스를 복구했다(360 닫기 · 382 열기 · 409 닫기 정상 짝).

**w5-4(이야기·사다리) 착수.** 남은 것은 w5-4 · w5-5.

---

## 2026-09-02 — W5.4 완료 (Andrew) · 리드 승인 · **E1~E4 에픽 완료**

푸시 `5b46922`. 테스트 125개 · check 0오류 · 2회 빌드 해시 동일(`8875d794…`).

### 폴백 사슬 — US-3 AC2를 정확히 구현

`derive/links.ts`: ① 직접(전부·발행 역순) → ② 태그(≤2) → ③ 버킷(≤2) → ④ none.
**첫 비지 않는 단계만** 표시.

리드 실측 — 테스트 6건이 AC를 그대로 반영:

| 테스트 | AC |
|---|---|
| ① 직접 참조 ≥1 → 전부, 발행 역순 | US-3 AC1 |
| ② **직접 0 + 태그 1 + 버킷 3 → 태그 1편만 (핵심 계약)** | US-3 AC2 — Matthew가 명시한 정확한 케이스 |
| ③ 태그도 0 → 같은 버킷 이야기 최대 2편 | US-3 AC2 |
| ④ 전부 0 → mode none (영역 미표시) | US-3 AC3 — 빈 껍데기 UI 금지 |
| 평론 있음 → 링크 / 없음·미등록 → 비링크 | 유사 링크 금지 |
| **소급: 평론이 나중에 발행되면 다음 도출에서 링크로 승격 (원 수정 0)** | D7 접점 상한 — 원이 추가 노동을 하지 않는다 |

마지막 항목이 이 제품의 설계 의도를 그대로 실현한다 — **연결은 자동으로 자라고 원은
글만 쓴다.**

### 계보 — 안 A 확정 구현, 안 B·C는 성장 경로로 보존

**원 무응답으로 안 A(글 끝 앨범 상자)를 기본 확정해 구현**했다(계획대로).
`AlbumBox.astro` + `LadderBlock.astro` 3모드 리드 카피.

**본문 자동 이름 매칭은 하지 않았다** — Matthew의 W3 해석(ui-spec §3 vs US-4 AC1의
"또는") 그대로, AlbumBox 링크로 AC를 충족한다. 오매칭 리스크를 피한 판단이 유지됐다.

**`LineageBlock`(안 B)은 예비 계약만 유지하고 미활성.** 리드 실측: `src/components/`에
파일 없음 — 실제로 만들지 않았다. **Joshua의 Non-goals("계보 전용 시각화 UI 1차 제외 —
데이터 층만", USP-B 수요 미검증)를 정확히 지켰다.** 안 B 확정 시 **반나절 규모**로 기록됐다.

> Jonnathan이 시안에 적은 *"셋은 경쟁이 아니라 같은 재료 위의 성장 경로 — 지금 고른 것이
> 나중을 막지 않습니다"* 가 구현에서 사실로 확인됐다. 안 A를 만들었어도 B·C가 열려 있다.

### 시계 재점검

4건 — 기존과 동일(checker `builtAt` · `currentYearMonthSeoul` · `mb/client` ×2).
**신규 누수 0.**

### 에픽 진행

**E1 · E2 · E3 · E4 완료.** 남은 것은 **w5-5(정체성·런칭) 하나** — 완료 시 최소 공개
단위(E1+E2+E5)를 넘어 전 에픽 완료 상태로 공개 게이트에 진입한다.

w5-5 착수.

---

# ⏸️ 일시정지 — 2026-09-02 (User 요청)

## 재개 브리핑 (다음 세션이 이것만 읽고 이어갈 수 있어야 한다)

### 현재 위치

**W5 구현 중 · w5-5가 마지막 스토리.** 개발분은 끝났고 **공개 게이트만 남았다.**

| 스토리 | status |
|---|---|
| w5-0 setup | ✅ done |
| w5-1 review-pipeline | ✅ done |
| w5-2 list-engine | ✅ done |
| w5-3 archive | ✅ done |
| w5-4 ladder | ✅ done |
| **w5-5 identity-launch** | **`review`** — 개발분 완료, 공개 게이트 잔여 |

**E1~E4 에픽 완료.** 테스트 125개+ · 시계 누수 0 · 매 스토리 2회 빌드 해시 동일.

### 게이트 이력 (전부 CONCERNS, FAIL 없음)

`Usp W1` ×2 (재실행) · `Plan W2` · `Implementation W3`

### 재개 시 첫 할 일

1. **`.agent-team/03-story-engineering/story-w5-5-identity-launch-kr.md`의 Dev Agent Record**
   — Andrew가 중단 지점과 다음 시작점을 기록해 뒀다. 이것이 재개의 입력이다
2. **`.agent-team/08-impl-notes/w5-5-launch-gate.md`** — 공개 게이트 자가 점검 문서
3. `git log --oneline -20`으로 최근 커밋 확인

### w5-5 잔여 3건 (Andrew 기록)

- **Matthias QA 전수** — 공개 게이트가 QA 검증을 요구한다
- **User 작업** — 아래 참조
- **성능 예산 협의** — 리드/User 판단 필요 항목

### 🔴 User 작업 (누적 · 미완)

1. **저장소 Settings → Pages → Source = `GitHub Actions`** — deploy 잡 차단 중.
   verify 게이트는 정상 동작하므로 개발엔 무관하나 **공개는 불가**
2. **Actions 탭 1회 확인** — 비공개 저장소 + `gh` CLI 부재로 원격 CI 결과 확인 불가.
   로컬 CI 전 스텝 동등 검증은 통과. (`winget install GitHub.cli` 후 `gh auth login`이면 자동화됨)

### 원(제품 소유자) 미결 3건 — 전부 비차단, 단 공개 전 확정 권장

| 항목 | 현재 처리 |
|---|---|
| **사이트 이름** | 미확정. `site_name` 설정값 + 워드마크 텍스트 로고라 **문자열 1곳 교체**. 공개 게이트에서 미정이면 어정쩡하게 나간다 |
| **계보 A/B/C** | 무응답 → **안 A로 확정 구현 완료.** 안 B 원하면 반나절 규모(`LineageBlock` 예비 계약 존재) |
| **Q1 장르 칸 수** | 무응답 → `config/genres.yaml` 2026 블록에 **B안 3버킷** 기본값. 값 교체만으로 변경 가능 |

### W6 이월 — 상류 문서 정정 2건 (구현이 찾아낸 계약 결함)

1. **E-101 문구** — `exceptions.md:124`가 아티스트 빈 본문을 실패로 잡으나 `api-contracts` §3.4가 명시 허용. 아티스트 제외 구현이 옳음
2. **`ArchiveItem` / `artist-intro`** — 해소 주석은 `api-contracts` §4.4에 기록됨. enum은 미사용 상태로 보존

> 둘 다 W1~W3 세 겹 검증을 통과한 문서에 있었다. **문서 대조로는 안 잡히고 구현해 봐야
> 드러나는 종류**다.

### 알아 둘 성질 1건 (W6에서 결함으로 오인하지 말 것)

`derive/lists.ts`의 **종료월 판정은 월 경계에서만 산출이 바뀌어 같은 달 2회 빌드로는
검출되지 않는다.** Thomas가 Footer ©에서 지적한 잠복 성질과 같아 보이나, **Footer ©는
사양 위반이었고 이것은 R-10이 명시 허용한 제품 행동**(월말정산은 월이 끝나야 나온다)이다.

### 살아 있는 팀원 (idle)

W1~W3 팀원 다수가 idle 상태로 남아 있다(John·Caleb·Caleb2·Joshua·James·Jonnathan·
James2·Jonnathan2·Matthew·Thomas·Matthias). 지시 없이는 아무것도 하지 않으므로 무해하나,
**재개 시 `/team-cleanup`으로 정리하는 것이 깔끔하다.**

> ⚠️ **재스폰 주의:** 팀원의 `failed`(세션 한도)는 종료가 아니다. 재스폰 전
> `ListAgents`로 생존을 확인할 것 — 이 세션에서 이중 가동 사고를 겪었다.

### 다음 단계

1. w5-5 공개 게이트 마감 (Matthias QA · User 작업 · 성능 예산)
2. `/wave6-verify-report` — Thomas·Timothy·Matthias → Michael(보안) → Hananiah(리팩터) → Martin(리포트)

## ⏸️ 정지 확정 — Andrew W5 구현 큐 종료 (2026-09-02)

**작업 트리 깨끗(미커밋 0) · 원격 동기화 완료 · 빌드 통과 상태.** 안전하게 멈췄다.

**최종: 13지면 · 테스트 125개 · astro check 0오류 · 2회 빌드 해시 동일(`12426efe…`).**

### Andrew가 `done`이 아니라 `review`로 둔 판단 — 옳다

w5-5의 AC7(Matthias US-1~15 전수 QA)이 자기 소유가 아니고 스토리에
*"QA를 넘기며 게이트 통과 처리 금지"* 가 명문이다. **`done` 선언은 완료 거짓말이 된다**는
이유로 스스로 `review`에 멈춘 것 — 이 프로젝트가 게이트마다 요구해 온 규율 그대로다.

### 📌 재개 시 리드가 결정할 것 3건

**① 성능 예산 (AC5 명문 — 협의 대상)**

초기 전송 실측: 홈 **754KB** · 평론 **1,067KB** vs 예산 **500KB**. 원인은 폰트 3면 전부
(HTML+CSS는 지면당 ~4KB에 불과, 폰트 합계 1,063KB는 1.5MB 캐시 예산 내이며 재방문 0KB).

- Andrew 권고: **1차 공개는 현 상태 수용**, 트래픽 실측 후 `unicode-range` 분할(반나절) 검토
- **리드 잠정 의견 (재개 시 확정):** 권고 수용 쪽. 근거 — ⓐ 예산 초과분이 전부 폰트이고
  **재방문 0KB**다 ⓑ 이 제품의 유입 축은 검색이 아니라 커뮤니티 유통·SNS(charter §3)이라
  첫 방문 이탈보다 **읽기 경험**이 중요하다 ⓒ charter가 "베이스라인 없는 목표치 금지"를
  규율로 삼아 왔는데 500KB도 실측 없이 정한 값이다. **다만 이는 예산을 지우자는 게
  아니라 실측 후 재설정하자는 것** — 트래픽 데이터 확보 시 재검토를 명시할 것

**② Matthias QA 배정** — US-1~15 전수 수동 확인. `npm run dev` + `content/` 픽스처 시드.
체크리스트는 `08-impl-notes/w5-5-launch-gate.md` §5.

**③ `site.yaml` §7 옵션 필드 2건**
- `early_stage_threshold` — **이미 승인·문서 반영 완료**(커밋 `89f866a`). Andrew가 Zod 필드만 켜면 됨
- `goatcounter_code` — **신규 요청. 미결.** 계측 도입 확정 시 §7 절차로 처리

### 계측 선정 (Andrew 조사)

**GoatCounter** — 무료 · 쿠키 0 · GDPR 고지 불요 · 스크립트 1개. 리퍼러로 **평론↔이야기
이동률** 측정 가능(charter §3 체류 지표의 실측 수단). GitHub Pages 내장 분석 없음이 확인돼
후보에서 소멸. 설치는 User 계정 + `site.yaml` 필드 대기.

### 🔴 User 작업 (공개 전 필수 · 선택 구분)

| | 항목 |
|---|---|
| **필수** | 저장소 Settings → Pages → Source = `GitHub Actions` |
| **필수** | 공개 직전 **픽스처 콘텐츠 제거 + 원의 실콘텐츠 투입** (제거 목록 문서화됨) |
| 권장 | CI Actions 탭 1회 확인 (`gh` CLI 설치 시 자동화) |
| 권장 | 사이트 이름 확정 → `site.yaml` 1곳 |
| 선택 | GoatCounter 가입 · 커스텀 도메인(전까지 서브패스라 `base` 설정 5분 작업) |

### 핵심 문서

- `08-impl-notes/frontend.md` — 스토리별 구현 결정 기록
- `08-impl-notes/w5-5-launch-gate.md` — **공개 게이트 체크리스트 (재개 시 본체 문서)**

## 재개 시작 순서 (Andrew 중단 기록 `0233ebd` — 리드 보강)

### ① `early_stage_threshold` Zod 필드 켜기 — 15분, 유일한 미완 코드 항목

§7 승인·문서 반영은 끝났으나(`89f866a`) **코드가 아직 상수다** —
리드 실측: `src/lib/derive/site-data.ts:37` `export const EARLY_STAGE_THRESHOLD = 6;`
Andrew가 파일·방법을 기록해 뒀다. 재개 첫 작업.

### ② 픽스처 잔존 빌드 게이트 — 설계 협의 필요 (리드 지시 미구현)

리드가 w5-1 검수에서 지시한 항목인데, **Andrew가 구현 전에 설계 문제를 발견했다:**

> 상시 검사로 하면 **현 개발 픽스처가 빌드를 막는 구조**가 된다.

그가 제시한 선택지: **(a) 릴리스 잡 한정** vs **(b) 공개 모드 설정 필드**

**리드 잠정 결론 — (a) 릴리스 잡 한정 (재개 시 확정):**

- 이 검사의 목적은 *"픽스처로 개발하지 마라"* 가 아니라 **"픽스처를 배포하지 마라"** 다.
  따라서 **배포 경계**에 두는 것이 목적과 정합한다
- (b)는 **상태를 하나 만든다** — 켜는 걸 잊으면 픽스처가 배포되고, 끄는 걸 잊으면 개발이
  막힌다. **실패 모드가 0개에서 2개로 늘어난다**
- (a)는 dev 빌드를 계속 초록으로 두면서 **실제로 나가는 것**만 지킨다. 배포 시점에
  드러나지만 배포는 게이트이므로 안전하게 실패한다
- 구현 시 **ADR-0009의 원자성 논거**(검증된 dist-1 바이트를 재빌드 없이 배포)를 깨지 말 것 —
  별도 빌드를 돌리지 말고 **배포 잡에서 `content/`의 `fixture-` 접두 존재를 확인**하는 형태가
  자연스럽다

> Andrew가 지시를 그대로 구현하지 않고 **설계 결함을 먼저 보고한 것이 옳다.**
> 리드 지시가 개발 흐름을 막는 부작용을 갖고 있었다.

### ③ 공개 게이트 잔여 — `08-impl-notes/w5-5-launch-gate.md` §5 그대로

### 정정 — `pret-rel.json`

Andrew는 잡 tmp에 받았다고 기억하나 실제로는 저장소 루트에 있었고, 리드가 커밋에 섞였다가
`39a9758`에서 추적 해제·삭제하고 `.gitignore` 패턴을 추가했다. **현재 루트에 없음(재확인).**

### 팀원 상태

Andrew도 세션 한도로 중단(리셋 **22:20 KST**). W1~W5 팀원 전원 idle 또는 한도 중단.
**재개 시 `ListAgents`로 생존 확인 후 필요한 역할만 재스폰할 것.**
